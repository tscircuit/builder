/**
 * The drawing builder is what is used to draw simple shapes, it's used by
 * the schematic-symbol-builder and the pcb-symbol-builder to construct simple
 * shapes with boxes, circles, lines and text.
 */

export const createDrawingBuilder = () => {
  throw new Error("createDrawingBuilder not implemented")
}
